# n8n 本地開發環境設置指南

本指南將幫助你設置 n8n 的本地開發環境，讓你能夠修改代碼並在瀏覽器 Console 中看到 `console.log` 的輸出。

## ✅ 完整設置 Checklist

### 📋 階段一：環境準備

- [ ] **1.1 停止並清理現有 Docker 容器**
  ```bash
  docker stop n8n
  docker rm n8n
  ```
  > 確認輸出顯示容器已停止和刪除

- [ ] **1.2 檢查 Node.js 版本**
  ```bash
  node --version
  ```
  > 必須 >= v22.16.0，如果版本不符，請先升級 Node.js

- [ ] **1.3 檢查當前所在目錄**
  ```bash
  pwd
  ```
  > 確認輸出為 n8n 項目的根目錄（應包含 `package.json`、`pnpm-workspace.yaml` 等文件）  
  > 如果不在項目根目錄，執行：`cd <your-n8n-project-path>`（替換為你的 n8n 專案路徑）

- [ ] **1.4 啟用 corepack**
  ```bash
  corepack enable
  ```
  > Mac 使用 Homebrew 安裝 Node.js 的用戶可能需要：`brew install corepack`

- [ ] **1.5 安裝正確的 pnpm 版本**
  ```bash
  corepack prepare --activate
  ```
  > 確認輸出顯示 pnpm 已準備就緒

- [ ] **1.6 驗證 pnpm 版本**
  ```bash
  pnpm --version
  ```
  > 必須 >= 10.22.0

---

### 📦 階段二：安裝依賴與構建

- [ ] **2.1 清理舊的依賴和構建（可選，首次安裝跳過）**
  ```bash
  pnpm clean
  ```
  > 如果之前安裝過依賴，建議執行此步驟

- [ ] **2.2 安裝所有專案依賴**
  ```bash
  pnpm install
  ```
  > ⏱️ 預計耗時：5-15 分鐘（取決於網路速度）  
  > ✅ 確認最後顯示安裝成功，無錯誤訊息  
  > ⚠️ 如果出現錯誤，檢查是否有權限問題或網路問題

- [ ] **2.3 構建所有套件**
  ```bash
  pnpm build
  ```
  > ⏱️ 預計耗時：10-20 分鐘（首次構建較慢）  
  > ✅ 確認所有套件構建成功  
  > ⚠️ 觀察終端輸出，不應該有 TypeScript 編譯錯誤

---

### ⚙️ 階段三：環境變數設置

- [ ] **3.1 創建環境變數文件**
  ```bash
  cat > .env << 'EOF'
  # 時區設定
  GENERIC_TIMEZONE=Asia/Taipei
  TZ=Asia/Taipei
  
  # 開發模式設定
  N8N_RUNNERS_ENABLED=true
  N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
  
  # 啟用節點熱重載
  N8N_DEV_RELOAD=true
  EOF
  ```
  > ✅ 執行後無錯誤訊息

- [ ] **3.2 驗證 .env 文件已創建**
  ```bash
  cat .env
  ```
  > ✅ 確認文件內容正確顯示

---

### 🚀 階段四：啟動開發模式

選擇以下**其中一個**選項：

#### 選項 A：完整開發模式（推薦用於全面開發）

- [ ] **4A.1 啟動完整開發模式**
  ```bash
  pnpm dev
  ```
  > ⏱️ 啟動時間：1-3 分鐘  
  > ✅ 等待直到看到類似以下訊息：
  > ```
  > VITE v5.x.x  ready in xxx ms
  > ➜  Local:   http://localhost:5678/
  > ```
  > 以及後端啟動訊息

- [ ] **4A.2 驗證應用運行正常**
  - 打開瀏覽器訪問：http://localhost:5678
  - ✅ 確認可以看到 n8n 登入/註冊頁面
  - ✅ 完成初始設置（如果是首次運行）

#### 選項 B：前端開發模式（適合只修改 UI）

- [ ] **4B.1 啟動後端服務**
  ```bash
  # 在終端 1 執行
  pnpm start
  ```
  > ⏱️ 等待後端啟動完成

- [ ] **4B.2 啟動前端開發服務器**
  ```bash
  # 在終端 2 執行
  cd packages/frontend/editor-ui
  pnpm dev
  ```
  > ✅ 等待 Vite 開發服務器啟動

- [ ] **4B.3 驗證應用運行正常**
  - 打開瀏覽器訪問：http://localhost:5678

#### 選項 C：後端開發模式（適合只修改後端邏輯）

- [ ] **4C.1 啟動後端開發模式**
  ```bash
  pnpm dev:be
  ```
  > ✅ 等待後端啟動完成

---

### 🐛 階段五：添加 Console Log 測試

#### 前端測試：

- [ ] **5.1 找到要測試的前端文件**
  ```bash
  # 範例：打開節點時的追蹤
  code packages/frontend/editor-ui/src/features/ndv/shared/views/NodeDetailsView.vue
  ```
  > 或使用你習慣的編輯器打開

- [ ] **5.2 添加 console.log**
  
  在 `watch(activeNode)` 中添加：
  ```typescript
  console.log('🔍 [DEV] Node opened:', {
    nodeName: node.name,
    nodeType: node.type,
    nodeId: node.id,
    timestamp: new Date().toISOString()
  });
  ```

- [ ] **5.3 保存文件**
  > ⚠️ 注意觀察終端，應該會顯示 HMR 更新訊息

- [ ] **5.4 在瀏覽器測試**
  - 打開瀏覽器開發者工具（F12 或 Cmd+Option+I）
  - 切換到 Console 標籤
  - 在 n8n 中點擊任何節點打開
  - ✅ 確認在 Console 看到你的 log 訊息

#### 後端測試：

- [ ] **5.5 找到要測試的後端文件**
  ```bash
  # 範例：工作流執行追蹤
  code packages/cli/src/workflows/workflow-execution.service.ts
  ```

- [ ] **5.6 添加 console.log**
  
  在 `executeManually` 方法開頭添加：
  ```typescript
  console.log('🚀 [DEV] Starting manual execution:', {
    workflowId: workflowData.id,
    workflowName: workflowData.name,
    userId: user.id,
    timestamp: new Date().toISOString()
  });
  ```

- [ ] **5.7 保存文件**
  > ⚠️ 觀察終端，nodemon 應該會自動重啟後端  
  > 等待重啟完成（約 5-10 秒）

- [ ] **5.8 在終端查看輸出**
  - 在 n8n 中執行任何工作流
  - ✅ 確認在運行 `pnpm dev` 的終端看到你的 log 訊息

---

### 🧪 階段六：驗證與測試

- [ ] **6.1 測試熱重載功能（前端）**
  - 修改任何前端文件（例如改變一個文字）
  - 保存
  - ✅ 確認瀏覽器自動刷新，變更立即生效
  - ✅ 不需要手動重新整理頁面

- [ ] **6.2 測試自動重啟功能（後端）**
  - 修改任何後端文件
  - 保存
  - ✅ 確認終端顯示 nodemon 重啟訊息
  - ✅ 重啟後應用可正常使用

- [ ] **6.3 測試完整工作流**
  - 創建一個簡單的工作流
  - 添加幾個節點
  - 執行工作流
  - ✅ 確認所有功能正常運作
  - ✅ 確認你的 console.log 按預期顯示

---

### 🎯 階段七：Production 模式驗證（可選）

- [ ] **7.1 停止開發模式**
  - 在終端按 `Ctrl+C` 停止 `pnpm dev`

- [ ] **7.2 重新構建**
  ```bash
  pnpm build
  ```
  > ⏱️ 預計耗時：5-10 分鐘（增量構建較快）

- [ ] **7.3 啟動 production 模式**
  ```bash
  pnpm start
  ```

- [ ] **7.4 測試應用**
  - 打開 http://localhost:5678
  - ✅ 確認應用正常運行
  - ⚠️ 注意：production 模式下前端的 console.log 可能被優化掉

- [ ] **7.5 停止 production 模式**
  - 按 `Ctrl+C` 停止

- [ ] **7.6 返回開發模式**
  ```bash
  pnpm dev
  ```

---

## 📝 常用指令快速參考

### 開發指令
```bash
pnpm dev           # 完整開發模式（前端 + 後端）
pnpm dev:fe        # 僅前端開發
pnpm dev:be        # 僅後端開發
pnpm dev:ai        # AI 節點開發
```

### 構建指令
```bash
pnpm build         # 構建所有套件
pnpm clean         # 清理構建產物
pnpm reset         # 完全重置（清理依賴和構建）
```

### 測試指令
```bash
pnpm test          # 運行所有測試
pnpm lint          # 程式碼檢查
pnpm format        # 程式碼格式化
```

### 啟動指令
```bash
pnpm start         # Production 模式啟動
pnpm start:tunnel  # 啟動並開啟 tunnel
```

---

## 🆘 故障排除

### 常見問題與解決方案

#### 問題 1：端口 5678 已被佔用
```bash
# 查看佔用端口的進程
lsof -i :5678

# 終止佔用端口的進程
kill -9 <PID>
```

#### 問題 2：Node.js 版本不符
```bash
# 使用 nvm 安裝正確版本
nvm install 22.16
nvm use 22.16
```

#### 問題 3：pnpm 安裝失敗
```bash
# 清理 pnpm store 和 node_modules
pnpm store prune
rm -rf node_modules
pnpm install
```

#### 問題 4：構建錯誤
```bash
# 完全清理並重新構建
pnpm clean
rm -rf node_modules
rm -rf packages/*/node_modules
pnpm install
pnpm build
```

#### 問題 5：熱重載不工作
- 確認使用的是 `pnpm dev` 而不是 `pnpm start`
- 檢查終端是否有錯誤訊息
- 嘗試重啟開發服務器

### 完整重置步驟

如果遇到無法解決的問題，可以完全重置環境：

```bash
# 1. 停止所有運行的進程
# 按 Ctrl+C 停止開發服務器

# 2. 清理所有構建產物和依賴
pnpm clean
rm -rf node_modules
rm -rf packages/*/node_modules
rm -rf packages/*/dist

# 3. 清理 pnpm store
pnpm store prune

# 4. 重新安裝和構建
pnpm install
pnpm build

# 5. 重新啟動
pnpm dev
```

---

## 📚 相關資源

- [n8n 官方文檔](https://docs.n8n.io)
- [貢獻指南](CONTRIBUTING.md)
- [n8n 社群論壇](https://community.n8n.io)
- [GitHub 問題追蹤](https://github.com/n8n-io/n8n/issues)

---

## ✨ 成功標準

完成以上所有步驟後，你應該能夠：

- ✅ 在瀏覽器訪問 http://localhost:5678
- ✅ 修改前端代碼後立即在瀏覽器看到變更（熱模組替換）
- ✅ 修改後端代碼後 nodemon 自動重啟
- ✅ 在瀏覽器 Console 看到前端的 console.log
- ✅ 在終端看到後端的 console.log
- ✅ 所有 n8n 功能正常運作

---

## 🎓 開發技巧

### 1. 選擇性開發策略

根據你的開發任務選擇最合適的模式：

**僅修改前端 UI**（最快）：
```bash
# 終端 1
pnpm start

# 終端 2
cd packages/frontend/editor-ui
pnpm dev
```

**僅修改某個節點**：
```bash
# 終端 1
cd packages/nodes-base
pnpm dev

# 終端 2
cd packages/cli
N8N_DEV_RELOAD=true pnpm dev
```

### 2. 調試技巧

#### 前端調試
- 使用 Vue DevTools 瀏覽器擴展
- 在 Chrome DevTools 中設置斷點
- 使用 `debugger` 語句暫停執行

#### 後端調試
- 在 VS Code 中設置斷點
- 使用 `--inspect` 標誌啟動 Node.js
- 查看終端輸出的詳細日誌

### 3. 效能優化

如果開發環境運行緩慢：
- 使用選擇性開發模式（僅運行需要的套件）
- 關閉不必要的應用程式
- 考慮增加系統 RAM
- 使用 SSD 存儲專案

### 4. Git 工作流程

```bash
# 1. 創建功能分支
git checkout -b feature/my-new-feature

# 2. 開發並測試
pnpm dev
# ... 進行修改 ...

# 3. 運行測試和 lint
pnpm test
pnpm lint

# 4. 提交變更
git add .
git commit -m "feat: add my new feature"

# 5. 推送到遠端
git push origin feature/my-new-feature
```

---

## 📌 注意事項

1. **不要提交 .env 文件**：`.env` 已在 `.gitignore` 中，確保不會被提交
2. **保持依賴更新**：定期運行 `pnpm install` 更新依賴
3. **測試你的變更**：在提交前運行 `pnpm test` 和 `pnpm lint`
4. **遵循代碼風格**：使用 `pnpm format` 格式化代碼
5. **查看 CONTRIBUTING.md**：了解更多貢獻指南

---

## 🎉 完成！

恭喜！你的 n8n 本地開發環境已經設置完成，現在可以開始開發了！

如果遇到任何問題，請參考：
- 本文檔的「故障排除」章節
- [CONTRIBUTING.md](CONTRIBUTING.md) 獲取更多開發資訊
- [n8n 社群論壇](https://community.n8n.io) 尋求幫助

祝開發順利！🚀

