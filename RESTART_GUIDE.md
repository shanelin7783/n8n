# 🔄 n8n 開發環境重啟指南

> 重開機後如何快速重新啟動 n8n 開發環境

## 🚀 快速啟動（推薦）

### 方式一：使用一鍵啟動腳本

```bash
cd <your-n8n-project-path>  # 切換到你的 n8n 專案目錄
./dev-start.sh
```

**說明**：
- 腳本會自動啟動後端和前端服務
- 等待約 45 秒後環境即可使用
- 訪問 http://localhost:8080 開始開發

### 方式二：手動啟動（更多控制）

```bash
# 1. 打開第一個終端 - 啟動後端
cd <your-n8n-project-path>/packages/cli
pnpm dev

# 2. 打開第二個終端 - 啟動前端
cd <your-n8n-project-path>/packages/frontend/editor-ui
pnpm dev
```

**等待時間**：
- 後端：約 30-40 秒
- 前端：約 15-20 秒

---

## ✅ 驗證服務已啟動

### 方法 1：檢查端口

```bash
# 檢查後端（應該看到端口 5678）
lsof -i :5678

# 檢查前端（應該看到端口 8080）
lsof -i :8080
```

### 方法 2：測試連接

```bash
# 測試後端 API
curl http://localhost:5678/healthz

# 應該返回：{"status":"ok"}
```

### 方法 3：打開瀏覽器

訪問 http://localhost:8080，應該看到 n8n 登入頁面

---

## 📋 查看日誌

### 使用腳本啟動的日誌位置

```bash
# 後端日誌
tail -f /tmp/n8n-backend.log

# 前端日誌
tail -f /tmp/n8n-frontend.log

# 同時查看兩個日誌
tail -f /tmp/n8n-backend.log /tmp/n8n-frontend.log
```

### 手動啟動時

直接在各自的終端查看輸出

---

## 🛑 停止服務

### 停止腳本啟動的服務

```bash
# 停止後端
pkill -f "nodemon.*n8n"

# 停止前端
pkill -f "vite.*8080"

# 或者一次性停止所有
pkill -f "nodemon.*n8n" && pkill -f "vite.*8080"
```

### 停止手動啟動的服務

在各自終端按 `Ctrl + C`

---

## ⚠️ 常見問題排查

### 問題 1：端口已被佔用

**錯誤訊息**：`Error: listen EADDRINUSE: address already in use`

**解決方案**：

```bash
# 1. 查找佔用端口的進程
lsof -i :5678  # 或 :8080

# 2. 終止進程（將 PID 替換為實際的進程 ID）
kill -9 <PID>

# 3. 或者直接終止所有相關進程
pkill -f "nodemon.*n8n"
pkill -f "vite.*8080"
```

### 問題 2：Node.js 版本錯誤

**錯誤訊息**：`The engine "node" is incompatible with this module`

**解決方案**：

```bash
# 檢查 Node.js 版本
node --version

# 應該是 >= v22.16.0
# 如果版本不符，使用 nvm 切換版本
nvm use 22
```

### 問題 3：pnpm 找不到

**錯誤訊息**：`command not found: pnpm`

**解決方案**：

```bash
# 啟用 corepack
corepack enable

# 安裝正確的 pnpm 版本
corepack prepare --activate

# 驗證
pnpm --version  # 應該是 >= 10.22.0
```

### 問題 4：編譯錯誤或模組找不到

**解決方案**：重新安裝依賴

```bash
cd <your-n8n-project-path>

# 清理
pnpm clean
rm -rf node_modules

# 重新安裝
pnpm install

# 重新構建
pnpm build
```

### 問題 5：數據庫遷移錯誤

**解決方案**：重置數據庫

```bash
# ⚠️ 注意：這會清空所有數據！
rm -rf ~/.n8n

# 重新啟動後端，會自動執行遷移
cd <your-n8n-project-path>/packages/cli
pnpm dev
```

---

## 🔧 完全重置（最後手段）

如果遇到無法解決的問題，可以完全重置：

```bash
cd <your-n8n-project-path>

# 1. 停止所有服務
pkill -f "nodemon.*n8n"
pkill -f "vite.*8080"

# 2. 清理所有構建產物和依賴
pnpm clean
rm -rf node_modules
rm -rf packages/*/node_modules
rm -rf packages/*/dist

# 3. 清理 pnpm store
pnpm store prune

# 4. 重新安裝和構建
pnpm install
pnpm build

# 5. 重新啟動
./dev-start.sh
```

---

## 📝 每日開發流程

### 1️⃣ 早上開始工作

```bash
# 進入專案目錄
cd <your-n8n-project-path>

# 啟動開發環境
./dev-start.sh

# 打開瀏覽器
open http://localhost:8080
```

### 2️⃣ 開發過程中

- **前端修改**：保存文件後自動熱重載 🔥
- **後端修改**：保存文件後自動重啟（約 5-10 秒）🔄
- **查看日誌**：
  - 前端：瀏覽器 Console (F12)
  - 後端：`tail -f /tmp/n8n-backend.log`

### 3️⃣ 下班離開

```bash
# 停止服務（可選，如果電腦不關機）
pkill -f "nodemon.*n8n" && pkill -f "vite.*8080"
```

---

## 🎯 測試賬號

**開發環境已創建的測試帳號：**

- **Email**: `dev@example.com`
- **Password**: `DevPassword123`
- **First Name**: `Dev`
- **Last Name**: `User`

> 💡 **提示**：如果數據庫被重置（刪除 `~/.n8n` 目錄），需要使用以上信息重新註冊。

---

## 💡 開發技巧

### 快捷指令（建議加入 .zshrc 或 .bashrc）

```bash
# 添加以下內容到 ~/.zshrc 或 ~/.bashrc（記得替換 <your-n8n-project-path> 為實際路徑）
alias n8n-start='cd <your-n8n-project-path> && ./dev-start.sh'
alias n8n-stop='pkill -f "nodemon.*n8n" && pkill -f "vite.*8080"'
alias n8n-log-be='tail -f /tmp/n8n-backend.log'
alias n8n-log-fe='tail -f /tmp/n8n-frontend.log'
alias n8n-logs='tail -f /tmp/n8n-backend.log /tmp/n8n-frontend.log'
```

重新加載配置：
```bash
source ~/.zshrc  # 或 source ~/.bashrc
```

使用：
```bash
n8n-start      # 啟動開發環境
n8n-stop       # 停止開發環境
n8n-log-be     # 查看後端日誌
n8n-log-fe     # 查看前端日誌
n8n-logs       # 同時查看兩個日誌
```

---

## 📚 相關文檔

- **完整設置指南**：[LOCAL_DEV.md](LOCAL_DEV.md)
- **設置完成報告**：[DEV_COMPLETE.md](DEV_COMPLETE.md)
- **官方文檔**：https://docs.n8n.io
- **貢獻指南**：[CONTRIBUTING.md](CONTRIBUTING.md)

---

## ✨ 總結

### 最簡單的重啟流程

```bash
cd <your-n8n-project-path>
./dev-start.sh
# 等待 45 秒
open http://localhost:8080
```

**就這麼簡單！** 🎉

如果遇到問題，參考上面的「常見問題排查」章節。

---

## 🆘 需要幫助？

1. 查看本文檔的「常見問題排查」章節
2. 查看 [LOCAL_DEV.md](LOCAL_DEV.md) 的「故障排除」章節
3. 檢查後端日誌：`tail -f /tmp/n8n-backend.log`
4. 檢查前端日誌：`tail -f /tmp/n8n-frontend.log`
5. 訪問 [n8n 社群論壇](https://community.n8n.io)

**祝開發順利！** 🚀

